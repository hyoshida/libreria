en:
  activerecord:
    state_machines:
      book:
        state:
          states:
            wished: 'Wished'
            ready: 'Ready'
            onloan: 'On loan'
          events:
            arrived: 'Arrived'
            loaned: 'Loaned'
            returned: 'Returned'
    errors:
      models:
        book:
          attributes:
            state:
              invalid_transition: 'cannot %{event}'
